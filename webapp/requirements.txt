# Flask web framework
flask==3.0.0
flask-cors==4.0.0

# Image processing
pillow==10.0.0

# OpenCV headless (no GUI dependencies)
opencv-python-headless==4.8.1.78

# Core ML dependencies
numpy>=1.21.0
matplotlib>=3.3.0

# PyTorch CPU-only for smaller deployment
torch>=2.0.0
torchvision>=0.15.0

# ML/AI Libraries (optional - app works without if import fails)
ultralytics>=8.0.0 