[phases.setup]
nixPkgs = [
  'python39',
  'libGL',
  'libglib',
  'glib',
  'libSM',
  'libXext',
  'libXrender',
  'libgomp'
]

[phases.install]
cmds = ['pip install -r requirements.txt']

[phases.build]  
cmds = ['echo "Build complete"']

[start]
cmd = 'python app.py' 